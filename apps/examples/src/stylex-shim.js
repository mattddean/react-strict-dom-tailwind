import { css } from 'react-strict-dom';

const attrs = css.attrs;
const create = css.create;
const createTheme = css.createTheme;
const defineVars = css.defineVars;
const firstThatWorks = css.firstThatWorks;
const include = css.include;
const keyframes = css.keyframes;
const legacyMerge = css.legacyMerge;
const props = css.props;
const types = css.types;

export {
  attrs,
  create,
  createTheme,
  defineVars,
  firstThatWorks,
  include,
  keyframes,
  legacyMerge,
  props,
  types
};

export default css.default;
